{
    // https://code.visualstudio.com/docs/editor/debugging
    // Shell redirects (e.g. > catalog.json) are not applied here—args go straight to Python.
    // After debug, redirect in the integrated terminal if you need files on disk.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "tap-concur discover",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/tap_concur/tap.py",
            "cwd": "${workspaceFolder}/.secrets",
            "args": [
                "--config",
                "config.json",
                "--discover",
                ">",
                "catalog.json"
            ],
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.venv/bin/python3"
        },
        {
            "name": "tap-concur sync",
            "type": "python",
            "request": "launch",
            "justMyCode": false,
            "program": "${workspaceFolder}/tap_concur/tap.py",
            "cwd": "${workspaceFolder}/.secrets",
            "args": [
                "--config",
                "config.json",
                "--state",
                "state.json",
                "--catalog",
                "catalog-selected.json",
                ">",
                "data.singer"
            ],
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.venv/bin/python3"
        }
    ]
}
